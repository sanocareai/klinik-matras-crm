#!/usr/bin/env bash
# OBSOLETE — JANGAN DIJALANKAN.
# Skrip lama (workflow ~/klinik-matras + git pull) tidak lagi valid: produksi berjalan dari release-directory
# (~/releases/klinik-matras/<sha8>) dan menjalankannya bisa menimpa fitur Delivery V2/B3.
# Gunakan branch release/delivery-control-r2 (scripts/release-delivery-control-production.sh).
echo "STOP: skrip ini OBSOLETE dan sengaja dinonaktifkan. Pakai release/delivery-control-r2." >&2
exit 1
